name: pyaedt_env
channels:
  - defaults
dependencies:
  - python==3.8
  - pip
  - pandas
  - numpy
  - matplotlib
  - ipython
  - notebook
  - pip:
    - pyvista
    - pyaedt
prefix: C:\Anaconda3\envs\pyaedt_env
# conda env create -f environment.yml